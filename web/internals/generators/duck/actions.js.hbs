import types from 'rdx/modules/{{ camelCase name }}/types';
import createAction from 'rdx/utils/createAction';

export default {
    get{{ pascalCase name }}: payload => createAction(types.GET_{{ constantCase name}}_REQUEST, payload),
    get{{ pascalCase name }}Success: payload => createAction(types.GET_{{ constantCase name}}_SUCCESS, payload),
    get{{ pascalCase name }}Failure: payload => createAction(types.GET_{{ constantCase name}}_FAILURE, payload),
};
