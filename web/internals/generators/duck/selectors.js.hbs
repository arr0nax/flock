import { get } from 'lodash';

export default {
    get{{ pascalCase name }}: state => get(state, '{{camelCase name}}.data'),
    get{{ pascalCase name }}Requested: state => get(state, '{{camelCase name}}.requested'),
    get{{ pascalCase name }}Error: state => get(state, '{{camelCase name}}.error'),
};
